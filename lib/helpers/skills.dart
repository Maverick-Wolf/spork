import 'package:flutter/material.dart';

class Skills extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.fromLTRB(20.0, 0.0, 0.0, 0.0),
      child: Text(
        "This sectione of the page is still under contruction (you saw the pun i pulled there \"under contruction\" cause i will always keep learning new skills, alright alright i'll stop, don't bully me)\nBut yeah i was just a bit lazy, will get it done soon, run 'projects' till then",
        style: TextStyle(color: Colors.white, fontSize: 16.0),
      ),
    );
  }
}
